node_modules/
*.log
.DS_Store
.codebuddy/
backend/_test_*.js
# ----- 上传目录：全部入库（2026-09-19 起）-----
# 用户自部署服务器（www.onlystyle.com.cn）靠 git pull 同步业务资产：
# logo_brand.*.png（官网导航+页脚+后台侧栏+登录页四处共用品牌图）、
# certs/（官网 about 页证书）、device_* / logo_*（业务线上传素材）。
# 服务器缺这些 = 图片 404（曾因「默认全忽略只放行 logo」导致部署缺图）。
#
# 🔴 历史教训（若将来恢复忽略必须遵守）：
#    必须写 uploads/* + !例外，不能写 uploads/ —— 写成 uploads/ 是
#    「排除整个目录」，git 根本不进这个目录，!例外 一起失效。
#    根级 .gitignore 里也不要重复写 biz-os/backend/uploads/。
# 🔴 文件名内容寻址（logo_brand.<sha256前8位>.png）：/uploads/* 带 immutable
#    一年缓存，换图必须换名才能让老访客看到。
*.backup
